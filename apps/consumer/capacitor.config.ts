import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.itaphoa.consumer',
  appName: 'Mio',
  webDir: '../../dist/apps/consumer/exported',
  bundledWebRuntime: false,
};

export default config;
