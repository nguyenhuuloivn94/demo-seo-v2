import styles from './Cart.module.scss';

export interface CartPartnerProps {
  data?: any;
}

export function CartPartner({ data }: CartPartnerProps) {
  return (
    <div style={{ marginTop: 55, paddingBottom: 85, backgroundColor: 'red' }}>
      123
    </div>
  );
}

export default CartPartner;

// export default Cart;
